server:
  port: 8080

app:
  coingecko:
    base-url: https://api.coingecko.com/api/v3
    demo-api-key: ${COINGECKO_DEMO_API_KEY:}
  agent:
    base-url: ${AGENT_BASE_URL:http://localhost:8001}

spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
